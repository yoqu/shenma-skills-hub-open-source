spring:
  datasource:
    url: ${SKILLSTACK_DB_URL:jdbc:mysql://localhost:3307/skillstack?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${SKILLSTACK_DB_USERNAME:skillstack}
    password: ${SKILLSTACK_DB_PASSWORD:skillstack}
    driver-class-name: com.mysql.cj.jdbc.Driver
